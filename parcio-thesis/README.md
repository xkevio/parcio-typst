- Spacing in paragraph between "Chapter X." heading and level 2 heading is not symmetrical due to usage of `1.25em` in block spacing while both have different font sizes
  - Prefer `_Large` font size (level 2 heading) for inter-block spacing (`set block(spacing: _Large * 1.25)`) for all headings

- Distance between "Chapter X." and heading body used to be `0.2em` instead of `0.25em`
  - Check both original parcio template and visually what is nicer
